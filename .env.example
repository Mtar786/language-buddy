# Example environment variables for the language buddy
#
# Copy this file to `.env` and provide real values when integrating with
# external translation services.

# API key for the translation service (e.g. Google Translate, DeepL, etc.)
TRANSLATION_API_KEY=your_translation_api_key

# Base URL or endpoint for the translation service
TRANSLATION_API_ENDPOINT=https://api.example.com/translate